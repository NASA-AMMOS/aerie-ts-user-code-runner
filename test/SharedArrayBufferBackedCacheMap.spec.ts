import { SharedArrayBufferBackedCacheMap } from '../src/SharedArrayBufferBackedCacheMap';

describe('SharedArrayBufferBackedCacheMap', () => {
	test('SharedArrayBufferBackedCacheMap#set', () => {
		const map = new SharedArrayBufferBackedCacheMap();
	});
});
